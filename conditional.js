// Input: A number to check
let number = -5;

// Using a conditional (ternary) operator to check the value
let result = (number > 0) 
    ? "The number is positive." 
    : (number < 0) 
        ? "The number is negative." 
        : "The number is zero.";

// Output the result
console.log(result);
