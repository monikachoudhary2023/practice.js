
function calculateMean(a, b, c, d, e) {
    
    let sum = a + b + c + d + e;
   
    let mean = sum / 5;
    return mean;
}

// Example usage:
let mean = calculateMean(10, 20, 30, 40, 50);
console.log(`The mean of the numbers is: ${mean}`);
